{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "ConnectionStrings": {
    "DefaultConnection": "Host=localhost;Database=catalogorderdb;Username=postgres;Password=postgres",
    "Redis": "localhost:6379"
  },
  "BlobStorage": {
    "ConnectionString": "UseDevelopmentStorage=true",
    "ContainerName": "design-variants"
  },
  "JwtSettings": {
    "SecretKey": "your-super-secret-key-min-32-characters-long-for-security",
    "Issuer": "CatalogOrderApi",
    "Audience": "CatalogOrderApiClients",
    "ExpirationMinutes": "1440"
  },
  "GoogleAuth": {
    "ClientId": "your-google-client-id.apps.googleusercontent.com"
  },
  "Cors": {
    "AllowedOrigins": [
      "*"
    ],
    "_comment": "⚠️ WARNING: Set specific origins for production! Wildcard (*) allows any origin and is a security risk. Example: ['https://yourdomain.com', 'https://app.yourdomain.com']"
  },
  "_security_notes": {
    "database_password": "⚠️ CHANGE the default PostgreSQL password before deployment",
    "jwt_secret": "⚠️ CHANGE the JWT SecretKey to a strong random value (min 32 characters)",
    "google_client_id": "⚠️ SET your actual Google OAuth Client ID",
    "blob_storage": "⚠️ CONFIGURE Azure Blob Storage connection string for production (not UseDevelopmentStorage)",
    "cors": "⚠️ RESTRICT CORS AllowedOrigins to specific domains in production"
  }
}
